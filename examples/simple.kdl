step "greet" {
  image "alpine:latest"
  run "echo 'Hello from Cicada!'"
}

step "build" {
  image "node:24-alpine"
  depends-on "greet"
  // KDL v2 raw string: double quotes inside need no escaping.
  run #"node -e 'console.log("hello from node")'"#
}
